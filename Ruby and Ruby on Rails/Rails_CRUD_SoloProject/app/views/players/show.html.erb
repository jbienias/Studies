<p id="notice" class="text-center"><%= notice %></p>

<div class="container">
    <div class="d-inline-block">
        <h2>Player: <%= @player.name %> "<%= @player.nickname %>" <%= @player.surname %></h2>
    </div>

    <hr />
    <p>
      <strong>Name:</strong>
      <%= @player.name %>
    </p>

    <p>
      <strong>Surname:</strong>
      <%= @player.surname %>
    </p>

    <p>
      <strong>Nickname:</strong>
      <%= @player.nickname %>
    </p>

    <p>
      <strong>Salary:</strong>
      <%= @player.salary %>
    </p>

    <p>
      <strong>Date of birth:</strong>
      <%= @player.date_of_birth.strftime("%d.%m.%Y") %>
    </p>
    <!-- COULD BE :
    <table>
      <tr>
        <td><strong>Name:</strong></td>
        <td><%= @player.name %></td>
      </tr>
      <tr>
        <td><strong>Surname:</strong></td>
        <td><%= @player.surname %></td>
      </tr>
      <tr>
        <td><strong>Nickname:</strong></td>
        <td><%= @player.nickname %></td>
      </tr>
      <tr>
        <td><strong>Salary:</strong></td>
        <td><%= @player.salary %></td>
      </tr>
      <tr>
        <td><strong>Date of birth:</strong></td>
        <td><%= @player.date_of_birth.strftime("%d.%m.%Y") %></td>
      </tr>
    </table>
    !-->
    <hr />

    <%= link_to 'Edit', edit_player_path(@player), :class => "btn btn-success btn-sm" %>
    <%= link_to 'Delete', @player, method: :delete, data: { confirm: 'Are you sure you want to delete this player?' }, :class => "btn btn-danger btn-sm" %>
    <%= link_to 'Back', players_path, :class => "btn btn-primary btn-sm" %>

</div>
